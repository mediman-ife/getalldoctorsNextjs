# Environment Variables for MediMan Doctor Directory

# Base URL for the application (used in SEO and sitemap)
NEXT_PUBLIC_BASE_URL=https://mediman.life

# API Configuration from Postman (fallback to default key if not set)
NEXT_PUBLIC_MEDIMAN_API_KEY=jmziDgOf+BmlBA8CJMkBT1hWAQltr1vh

# Cron Secret for cache revalidation (generate a secure random string)
CRON_SECRET=your_secure_cron_secret_here

# Optional: Analytics and monitoring (add if needed)
# NEXT_PUBLIC_GA_ID=your_google_analytics_id
# NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn